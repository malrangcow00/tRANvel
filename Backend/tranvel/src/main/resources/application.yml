spring:
  datasource:
    url: jdbc:mysql://localhost:3306/your_database?serverTimezone=UTC
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.example.com
    port: 587
    username: gumissafy00@example.com
    password: rsruvchbezixaizh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

#  security:
#    user:
#      name: admin
#      password: admin

jwt:
  secret: YourJWTSecretKey
  expiration: 604800 # 7 days in seconds
  token-prefix: "Bearer "
  header-string: "Authorization"

